services:
  # PostgreSQL Database
  db:
    image: postgres:15-alpine
    container_name: sudungthuoc_db
    environment:
      POSTGRES_USER: ${POSTGRES_SUPERUSER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD:?POSTGRES_SUPERUSER_PASSWORD is required}
      POSTGRES_DB: ${POSTGRES_DB:-sudungthuoc_db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - sudungthuoc_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_SUPERUSER:-postgres} -d ${POSTGRES_DB:-sudungthuoc_db}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  db-bootstrap:
    image: postgres:15-alpine
    depends_on:
      db:
        condition: service_healthy
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-sudungthuoc_db}
      POSTGRES_SUPERUSER: ${POSTGRES_SUPERUSER:-postgres}
      POSTGRES_SUPERUSER_PASSWORD: ${POSTGRES_SUPERUSER_PASSWORD:?POSTGRES_SUPERUSER_PASSWORD is required}
      APP_DB_USER: ${APP_DB_USER:?APP_DB_USER is required}
      APP_DB_PASSWORD: ${APP_DB_PASSWORD:?APP_DB_PASSWORD is required}
    volumes:
      - ./scripts/docker:/scripts:ro
    entrypoint: ["/bin/sh", "/scripts/bootstrap-app-db-role.sh"]
    networks:
      - sudungthuoc_network
    restart: "no"

  # Next.js Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sudungthuoc_app
    environment:
      DATABASE_URL: ${APP_DATABASE_URL:?APP_DATABASE_URL is required}
      AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET is required}
      AUTH_URL: ${AUTH_URL:-https://khoduoc.nvdcantho.com}
      REPORT_UPLOAD_SIGNING_SECRET: ${REPORT_UPLOAD_SIGNING_SECRET:?REPORT_UPLOAD_SIGNING_SECRET is required}
      DRUG_ORDER_QR_SIGNING_SECRET: ${DRUG_ORDER_QR_SIGNING_SECRET:?DRUG_ORDER_QR_SIGNING_SECRET is required}
      AI_PRIMARY_PROVIDER: ${AI_PRIMARY_PROVIDER:-google}
      AI_PRIMARY_MODEL: ${AI_PRIMARY_MODEL:-gemma-4-26b-a4b-it}
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      AI_ENABLE_FALLBACK: ${AI_ENABLE_FALLBACK:-false}
      AI_FALLBACK_PROVIDER: ${AI_FALLBACK_PROVIDER:-openai}
      AI_FALLBACK_MODEL: ${AI_FALLBACK_MODEL:-gpt-5.4-mini}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      AI_MAX_OUTPUT_TOKENS: ${AI_MAX_OUTPUT_TOKENS:-1200}
      AI_PROVIDER_TIMEOUT_MS: ${AI_PROVIDER_TIMEOUT_MS:-30000}
      AI_ADMIN_CHAT_DAILY_LIMIT: ${AI_ADMIN_CHAT_DAILY_LIMIT:-80}
      AI_ADMIN_REVIEW_DAILY_LIMIT: ${AI_ADMIN_REVIEW_DAILY_LIMIT:-40}
      AI_FACILITY_CHAT_DAILY_LIMIT: ${AI_FACILITY_CHAT_DAILY_LIMIT:-30}
      AI_FACILITY_REVIEW_DAILY_LIMIT: ${AI_FACILITY_REVIEW_DAILY_LIMIT:-15}
      NODE_ENV: "production"
      ALLOW_PRODUCTION_SEED: ${ALLOW_PRODUCTION_SEED:-false}
      SEED_ADMIN_USERNAME: ${SEED_ADMIN_USERNAME:-admin}
    depends_on:
      db:
        condition: service_healthy
      db-bootstrap:
        condition: service_completed_successfully
    networks:
      - sudungthuoc_network
    restart: unless-stopped

  app-admin:
    profiles: ["admin"]
    build:
      context: .
      dockerfile: Dockerfile
      target: builder
    environment:
      DATABASE_URL: ${DATABASE_ADMIN_URL:?DATABASE_ADMIN_URL is required}
      AUTH_SECRET: ${AUTH_SECRET:?AUTH_SECRET is required}
      AUTH_URL: ${AUTH_URL:-https://khoduoc.nvdcantho.com}
      REPORT_UPLOAD_SIGNING_SECRET: ${REPORT_UPLOAD_SIGNING_SECRET:?REPORT_UPLOAD_SIGNING_SECRET is required}
      DRUG_ORDER_QR_SIGNING_SECRET: ${DRUG_ORDER_QR_SIGNING_SECRET:?DRUG_ORDER_QR_SIGNING_SECRET is required}
      AI_PRIMARY_PROVIDER: ${AI_PRIMARY_PROVIDER:-google}
      AI_PRIMARY_MODEL: ${AI_PRIMARY_MODEL:-gemma-4-26b-a4b-it}
      GOOGLE_GENERATIVE_AI_API_KEY: ${GOOGLE_GENERATIVE_AI_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      AI_ENABLE_FALLBACK: ${AI_ENABLE_FALLBACK:-false}
      AI_FALLBACK_PROVIDER: ${AI_FALLBACK_PROVIDER:-openai}
      AI_FALLBACK_MODEL: ${AI_FALLBACK_MODEL:-gpt-5.4-mini}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      AI_MAX_OUTPUT_TOKENS: ${AI_MAX_OUTPUT_TOKENS:-1200}
      AI_PROVIDER_TIMEOUT_MS: ${AI_PROVIDER_TIMEOUT_MS:-30000}
      AI_ADMIN_CHAT_DAILY_LIMIT: ${AI_ADMIN_CHAT_DAILY_LIMIT:-80}
      AI_ADMIN_REVIEW_DAILY_LIMIT: ${AI_ADMIN_REVIEW_DAILY_LIMIT:-40}
      AI_FACILITY_CHAT_DAILY_LIMIT: ${AI_FACILITY_CHAT_DAILY_LIMIT:-30}
      AI_FACILITY_REVIEW_DAILY_LIMIT: ${AI_FACILITY_REVIEW_DAILY_LIMIT:-15}
      NODE_ENV: "production"
      ALLOW_PRODUCTION_SEED: ${ALLOW_PRODUCTION_SEED:-false}
      SEED_ADMIN_USERNAME: ${SEED_ADMIN_USERNAME:-admin}
    depends_on:
      db:
        condition: service_healthy
      db-bootstrap:
        condition: service_completed_successfully
    command: ["tail", "-f", "/dev/null"]
    networks:
      - sudungthuoc_network
    restart: "no"

  # Cloudflare Tunnel
  tunnel:
    image: cloudflare/cloudflared:latest
    container_name: sudungthuoc_tunnel
    command: tunnel --no-autoupdate run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      - app
    networks:
      - sudungthuoc_network
    restart: unless-stopped

networks:
  sudungthuoc_network:
    driver: bridge

volumes:
  postgres_data:
